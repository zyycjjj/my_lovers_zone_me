# Memory 数据库设计记录：阶段 1

## 1. 本次目标

本次数据库设计只服务第一阶段闭环：

`落地页 -> 号码认证登录 -> 自动建账号 -> 首次建档 -> 登录后分流 -> 进入个人空间`

目标不是一次性把所有业务表建完，而是先把正式商用账号体系落地，为后续 AI、订阅、资产、支持关系提供稳定底座。

## 2. 设计原则

- 新增商用账号体系，不直接复用旧 token 用户表
- 保留旧表，避免第一阶段把现有接口链路一起打断
- 先做账号、空间、会话、建档，不提前塞入支付、AI、支持关系等重业务表
- 登录方式要可扩展，不能把阿里云号码认证写死成唯一身份结构

## 3. 现有旧模型说明

当前数据库仍保留以下早期模型：

- `User`
- `Signal`
- `Event`
- `Echo`
- `Photo`

这些模型服务的是早期 token 访问逻辑，本次不删除、不改字段，只作为历史链路保留。

## 4. 本次新增模型

### 4.1 Account

账号主表，表示平台中的正式用户。

关键字段：

- `phone`
- `phoneVerifiedAt`
- `displayName`
- `avatarUrl`
- `status`

设计说明：

- `phone` 允许为空，是为了兼容后续扩展其他登录方式
- 正式手机号仍保持唯一
- `displayName` 放账号基础表，适合存放通用展示名

### 4.2 AuthIdentity

登录身份表，用于支持多种登录来源。

关键字段：

- `accountId`
- `provider`
- `providerUserId`
- `providerPhone`
- `isPrimary`

设计说明：

- 第一阶段主登录方式是 `aliyun_number_auth`
- 后续可无痛扩展 `sms_code / wechat / email`
- `providerPhone` 保留供应商返回手机号，方便排查认证问题

### 4.3 AuthSession

登录会话表，用于正式登录态管理。

关键字段：

- `sessionToken`
- `refreshToken`
- `expiredAt`
- `lastActiveAt`
- `ip`
- `userAgent`

设计说明：

- 第一阶段即切离旧 token 模式
- 支持后续多设备登录、登出、踢下线、刷新会话

### 4.4 Workspace

工作空间表，是后续所有业务归属的核心容器。

关键字段：

- `name`
- `type`
- `ownerAccountId`
- `status`

设计说明：

- 第一阶段就先引入空间概念
- 默认新账号自动创建一个 `personal` 空间
- 后续团队版直接扩为 `team`

### 4.5 WorkspaceMember

空间成员表，用于管理账号与空间的关系。

关键字段：

- `workspaceId`
- `accountId`
- `role`
- `status`
- `joinedAt`

设计说明：

- 第一阶段个人空间里默认只有一条 `owner`
- 后续团队、邀请、席位都基于这张表扩展

### 4.6 UserProfile

首次建档表，用于存放业务型用户画像信息。

关键字段：

- `accountId`
- `workspaceId`
- `nickname`
- `industry`
- `contentDirection`
- `targetPlatform`
- `experienceLevel`
- `onboardingCompletedAt`

设计说明：

- 把“账号基础信息”和“业务建档信息”分开
- 登录后分流时，主要依靠是否存在 `UserProfile` 或 `onboardingCompletedAt`

## 5. 本次新增枚举

- `AccountStatus`
  - `active`
  - `disabled`

- `AuthProvider`
  - `aliyun_number_auth`
  - `sms_code`
  - `wechat`
  - `email`

- `WorkspaceType`
  - `personal`
  - `team`

- `WorkspaceStatus`
  - `active`
  - `archived`

- `WorkspaceMemberRole`
  - `owner`
  - `admin`
  - `editor`

- `WorkspaceMemberStatus`
  - `active`
  - `invited`
  - `removed`

## 6. 关键关系设计

- `Account 1 - n AuthIdentity`
- `Account 1 - n AuthSession`
- `Account 1 - n Workspace(owned)`
- `Workspace 1 - n WorkspaceMember`
- `Account 1 - n WorkspaceMember`
- `Account 1 - 1 UserProfile`
- `Workspace 1 - n UserProfile`

## 7. 第一阶段登录与分流逻辑

### 7.1 首次登录

用户通过阿里云号码认证成功后：

1. 根据手机号查找 `Account`
2. 若不存在，则创建 `Account`
3. 创建 `AuthIdentity(provider = aliyun_number_auth)`
4. 自动创建一个 `Workspace(type = personal)`
5. 自动创建一条 `WorkspaceMember(role = owner)`
6. 创建 `AuthSession`

### 7.2 登录后分流

- 没有 `UserProfile`：进入首次建档
- 有 `UserProfile` 且只有一个空间：进入个人工作台
- 后续扩展多空间后：进入空间选择页

## 8. 本次有意不进入的表

以下模型已明确后置，不在第一阶段 schema 中落地：

- `SupportRelation`
- `Plan`
- `Subscription`
- `Order`
- `PaymentRecord`
- `AiTask`
- `ProductProfile`
- `Template`
- `ContentAsset`
- `UploadedFile`

原因：

- 它们不是第一阶段登录闭环的必要条件
- 现在提前加入只会增加迁移复杂度和实现负担

## 9. 本次 Prisma 变更范围

本次实际修改：

- 新增 6 个枚举
- 新增 6 张核心表
- 保留旧表 `User / Signal / Event / Echo / Photo` 不动

## 10. 后续建议顺序

完成这次 schema 后，建议按这个顺序继续推进：

1. 生成 Prisma Client
2. 写账号与会话模块
3. 接入阿里云号码认证登录接口
4. 写首次建档接口
5. 写登录后分流接口
6. 再进入第二阶段 AI 主流程
