# Memory 第一阶段接口测试报告

## 1. 测试目标

验证第一阶段新接口对应的核心业务链路是否符合当前产品设计：

- 号码认证登录
- 自动建账号与个人空间
- 首次建档
- 登录后分流
- 工作空间读取
- 新会话鉴权

本次测试以单元测试为主，目标是先锁定核心业务判断和关键分支。

## 2. 本次新增测试

### Auth

- `src/auth/domain/auth.domain.spec.ts`

覆盖点：

- 新手机号首次登录时自动创建账号、身份、个人空间和会话
- 已完成建档时登录后返回 `workspace_home`
- session 不存在时刷新会话失败

### Onboarding

- `src/onboarding/onboarding.domain.spec.ts`

覆盖点：

- 未建档状态下返回 `onboarding`
- 提交建档后返回 `workspace_home`
- session 失效时拒绝访问

### Workspace

- `src/workspace/workspace.domain.spec.ts`

覆盖点：

- 返回当前工作空间摘要
- 无空间成员关系时拒绝访问

### Session Guard

- `src/auth/guards/session-auth.guard.spec.ts`

覆盖点：

- session 有效时写入 `sessionToken / accountId / workspaceId`
- session 缺失时拒绝访问

### Controller

- `src/auth/auth.controller.spec.ts`
- `src/onboarding/onboarding.controller.spec.ts`
- `src/workspace/workspace.controller.spec.ts`

覆盖点：

- controller 是否正确读取 `request.sessionToken`
- controller 是否正确把参数透传给 service
- controller 返回结果是否保持业务对象结构，便于后续由统一拦截器包裹

### API 格式统一层

- `src/common/api-response.interceptor.spec.ts`
- `src/common/api-exception.filter.spec.ts`

覆盖点：

- 成功结果是否统一包装为 `code / message / requestId / timestamp / data`
- 参数校验错误是否统一包装为 `VALIDATION_ERROR`
- 未授权错误是否统一包装为 `UNAUTHORIZED`
- 未知错误是否统一包装为 `INTERNAL_ERROR`

## 3. 测试方式

- 测试类型：单元测试、接口层单元测试
- 测试框架：Jest
- 验证范围：领域逻辑、守卫逻辑、controller 调用链、统一响应格式

## 4. 当前结论

本次测试重点验证了第一阶段接口背后的核心决策逻辑，而不是前端联调或真实阿里云号码认证调用。

当前可以确认：

- 第一阶段的登录闭环核心判断已被测试覆盖
- 首次建档与登录后分流逻辑已被测试覆盖
- 工作空间读取和新会话守卫逻辑已被测试覆盖
- controller 到 service 的接口调用链已被测试覆盖
- 第一阶段新增接口的统一成功返回结构和统一错误结构已被测试覆盖

## 5. 仍未覆盖的内容

- 真实阿里云号码认证 OpenAPI 联调
- 与真实数据库的集成测试
- 与前端 SDK 的联调测试
- 基于真实 HTTP 适配器的 e2e 测试

## 6. 建议下一步

在当前单测通过后，下一步建议做两件事：

1. 针对 `/api/auth/number-auth-token` 和 `/api/auth/number-login` 做真实联调
2. 在前端接入前，补一轮真实数据库集成测试
3. 前后端联调时按统一错误码完成页面提示映射
