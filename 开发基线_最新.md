# Memory 后端开发基线

更新时间：2026-04-24

这份文档是当前后端阶段唯一保留的最新文档，用来统一记录三件事：

- 当前产品基线
- 最新开发任务顺序
- 第一阶段最终数据库设计

## 1. 当前产品基线

当前产品已经按 `Memory_Product_V3` 的口径收敛为三条主线：

- 爆款复刻：增长核心
- AI 编排：体验核心
- 留存机制：商业核心

产品不再只是一次性生成工具，而是一个面向中小商家、带货创作者和小团队的 AI 电商内容经营工作台。

## 2. 最新开发任务顺序

### 已完成：阶段 1 登录与账户底座

本阶段已经完成：

- 正式账号体系
- 阿里云号码认证主登录
- 短信验证码备用登录预留
- 会话体系
- 个人工作空间自动创建
- 首次建档基础流程
- 登录后分流
- 新接口统一返回格式和错误码
- 第一阶段核心测试

### 接下来按这个顺序继续开发

1. AI 文本主流程 + 爆款复刻闭环  
   目标：把“爆款复刻 / 商品生成 / 自由输入”三入口做成真正可用的生成中心。

2. 支付 + 内容库闭环  
   目标：让用户既能付费拿到完整结果，也能把结果沉淀成资产。当前已完成内容资产 MVP 起步。

3. AI 陪跑与留存闭环  
   目标：补齐 Check-in、今日任务、周总结和回访机制。

4. 图片生成与素材闭环  
   目标：支持上传商品图、生成宣传图、进入资产系统。

5. 支持关系闭环  
   目标：让轻关系反馈进入经营流程，但不打扰主工作台。

6. 视频生成闭环  
   目标：在图片和文本稳定后，补高成本的视频能力。

7. 团队与后台闭环  
   目标：补齐团队协作、席位、后台运营和风控。

## 3. 第一阶段最终数据库设计

### 3.1 设计目标

第一阶段只服务这条闭环：

`落地页 -> 登录 -> 自动建账号 -> 首次建档 -> 登录后分流 -> 进入个人空间`

目标不是一次性把所有业务表建完，而是先把正式商用账户底座搭稳。

### 3.2 核心表

#### `Account`

正式账号主表。

关键字段：

- `phone`
- `phoneVerifiedAt`
- `displayName`
- `avatarUrl`
- `status`

#### `AuthIdentity`

登录身份表，用于支持多种登录来源。

关键字段：

- `accountId`
- `provider`
- `providerUserId`
- `providerPhone`
- `isPrimary`

当前已支持的 provider：

- `aliyun_number_auth`
- `sms_code`
- `wechat`
- `email`

#### `AuthSession`

正式登录会话表。

关键字段：

- `sessionToken`
- `refreshToken`
- `expiredAt`
- `lastActiveAt`
- `ip`
- `userAgent`

#### `Workspace`

工作空间主表。

关键字段：

- `name`
- `type`
- `ownerAccountId`
- `status`

当前空间类型：

- `personal`
- `team`

#### `WorkspaceMember`

账号和工作空间的关系表。

关键字段：

- `workspaceId`
- `accountId`
- `role`
- `status`
- `joinedAt`

当前角色：

- `owner`
- `admin`
- `editor`

#### `UserProfile`

首次建档表，是第一阶段这次最终补强后的结果。

关键字段：

- `accountId`
- `workspaceId`
- `nickname`
- `businessRole`
- `industry`
- `currentGoal`
- `contentDirection`
- `targetPlatform`
- `experienceLevel`
- `onboardingCompletedAt`

字段解释：

- `businessRole`：身份选择，例如个体商家、带货博主、直播运营
- `industry`：当前主营类目
- `currentGoal`：当前阶段目标
- `contentDirection`：当前内容方向
- `targetPlatform`：目标平台
- `experienceLevel`：经验阶段

### 3.3 当前第一阶段建档结论

第一阶段建档不再只是“昵称 + 行业”，而是最少承接下面这组产品信息：

- 我是谁：`businessRole`
- 我主要做什么：`industry`
- 我现在最想解决什么：`currentGoal`
- 我打算怎么做内容：`contentDirection`
- 我主要发在哪里：`targetPlatform`

这已经能承接当前 PRD 里的：

- 身份选择
- 主营类目
- 当前目标

### 3.4 第一阶段有意后置的表

当前仍然明确后置，不在阶段 1 schema 中落地：

- `SupportRelation`
- `Plan`
- `Subscription`
- `Order`
- `PaymentRecord`
- `AiTask`
- `ExplosionCase`
- `ContentPerformance`
- `DailyTask`
- `ProductProfile`
- `Template`
- `ContentAsset`（已进入内容资产 MVP）
- `UploadedFile`

原因：

- 它们不是“登录和建档底座”必需表
- 现在提前落库会让第一阶段变胖
- 按闭环推进更稳

### 3.6 当前新增的内容资产 MVP 表

#### `ContentAsset`

生成结果沉淀表，用于先承接“保存内容、标记完成、昨日记录、次日继续提示”的最小闭环。

关键字段：

- `userId`
- `accountId`
- `workspaceId`
- `toolKey`
- `title`
- `content`
- `sourcePrompt`
- `status`
- `completedAt`

当前状态：

- `saved`
- `completed`
- `archived`

### 3.5 迁移记录

当前阶段实际已应用的关键 migration：

- `20260401071136_stage1_account_foundation`
- `20260402054333_add_onboarding_business_role_goal`
- `20260424090000_add_content_assets`

其中第二个 migration 已把以下字段加到远程数据库：

- `UserProfile.businessRole`
- `UserProfile.currentGoal`

## 4. 第一阶段接口基线

### 4.1 当前新接口

- `GET /api/auth/number-auth-token`
- `POST /api/auth/number-login`
- `GET /api/auth/me`
- `GET /api/auth/routing`
- `POST /api/auth/session/refresh`
- `POST /api/auth/logout`
- `GET /api/onboarding/status`
- `POST /api/onboarding/profile`
- `GET /api/workspaces`
- `GET /api/workspaces/current`

### 4.2 统一响应规范

成功结构：

```json
{
  "code": "SUCCESS",
  "message": "请求成功",
  "requestId": "req_xxx",
  "timestamp": "2026-04-02T10:00:00.000Z",
  "data": {}
}
```

错误结构：

```json
{
  "code": "UNAUTHORIZED",
  "message": "登录会话不存在",
  "requestId": "req_xxx",
  "timestamp": "2026-04-02T10:00:00.000Z",
  "details": []
}
```

当前统一错误码：

- `SUCCESS`
- `VALIDATION_ERROR`
- `BAD_REQUEST`
- `UNAUTHORIZED`
- `FORBIDDEN`
- `NOT_FOUND`
- `UPSTREAM_ERROR`
- `HTTP_ERROR`
- `INTERNAL_ERROR`

## 5. 当前验证结果

当前已经验证通过：

- `pnpm build`
- 第一阶段 9 组测试、22 条用例

覆盖范围包括：

- auth domain
- onboarding domain
- workspace domain
- session guard
- controller 级调用链
- 统一成功返回格式
- 统一错误返回格式

## 6. 这份文档替代的旧文档

这份文档已经替代以下阶段文档：

- `数据库设计_阶段1.md`
- `接口设计_阶段1.md`
- `测试报告_阶段1.md`

后续如果再迭代，直接继续更新这份文档，不再并行新增同类阶段文档。
