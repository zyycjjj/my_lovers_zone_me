# Memory 接口设计记录：阶段 1

## 1. 本阶段目标

本阶段接口只服务第一批正式商用闭环：

`落地页 -> 号码认证登录 -> 自动建账号 -> 首次建档 -> 登录后分流 -> 进入个人空间`

本次接口设计遵循以下分层约定：

- `repository`：数据库原子操作
- `domain`：业务规则和业务组合
- `service`：组织方法调用，对 controller 提供能力
- `controller`：接口定义、请求参数、响应结构、OpenAPI 描述

当前目录约定：

- 一个模块如果只有一个 `controller / service / domain` 文件，则直接放在模块根目录
- `dto` 和 `repositories` 只有在确实有多个文件时才保留子目录
- `auth` 模块额外保留少量根目录辅助文件，用于号码认证客户端和鉴权逻辑，不再单独拆 `module` 为两层

## 1.1 统一接口返回规范

第一阶段新增接口统一使用以下成功结构：

```json
{
  "code": "SUCCESS",
  "message": "请求成功",
  "requestId": "req_xxx",
  "timestamp": "2026-04-01T16:30:00.000Z",
  "data": {}
}
```

第一阶段新增接口统一使用以下错误结构：

```json
{
  "code": "UNAUTHORIZED",
  "message": "登录会话不存在",
  "requestId": "req_xxx",
  "timestamp": "2026-04-01T16:30:00.000Z",
  "details": []
}
```

错误码约定：

- `SUCCESS`：请求成功
- `VALIDATION_ERROR`：参数校验失败
- `BAD_REQUEST`：普通错误请求
- `UNAUTHORIZED`：未登录或登录会话失效
- `FORBIDDEN`：无权限访问
- `NOT_FOUND`：资源不存在
- `UPSTREAM_ERROR`：上游服务异常，例如号码认证服务失败
- `HTTP_ERROR`：未单独细分的 HTTP 错误
- `INTERNAL_ERROR`：服务内部错误

说明：

- 第一阶段新接口的成功状态码统一返回 `200`
- `requestId` 用于串联前端日志、后端日志和错误排查
- `details` 仅在校验错误等需要返回细节时出现

## 2. 模块划分

### 2.1 auth

负责：

- 阿里云号码认证授权 token 获取
- 号码认证登录
- 会话获取
- 会话刷新
- 退出登录
- 登录后分流

### 2.2 onboarding

负责：

- 首次建档状态查询
- 首次建档提交

### 2.3 workspace

负责：

- 当前工作空间
- 我的工作空间列表

## 3. 鉴权设计

### 3.1 旧鉴权

旧接口继续沿用：

- `x-user-token`

对应旧模型：

- `User`
- `Signal`
- `Event`
- `Echo`
- `Photo`

### 3.2 新鉴权

新阶段接口使用：

- `x-session-token`

也支持后续扩展 cookie 形式的 `session_token`。

## 4. 接口列表

## 4.1 Auth

### `GET /api/auth/number-auth-token`

用途：

- 获取阿里云 H5 号码认证所需的授权 token
- 给前端 JS SDK 初始化使用

返回：

- `accessToken`
- `jwtToken`
- `accessTokenExpiredAt`
- `jwtTokenExpiredAt`

说明：

- 这是为了适配阿里云 H5 号码认证流程补充的接口
- 不属于普通短信验证码流程

### `POST /api/auth/number-login`

用途：

- 前端完成 H5 号码认证后，将 `SpToken` 传给后端
- 后端完成取号、账号创建、空间创建、会话创建

请求体：

- `spToken`

返回：

- `account`
- `session`
- `routing`

成功状态码：

- `200`

业务规则：

- 如果手机号未注册，则自动注册账号
- 自动创建个人空间
- 自动创建 owner 成员关系

### `GET /api/auth/me`

用途：

- 获取当前登录态

鉴权：

- `x-session-token`

返回：

- `account`
- `currentWorkspace`
- `onboardingCompleted`

### `GET /api/auth/routing`

用途：

- 获取登录后分流结果

鉴权：

- `x-session-token`

返回：

- `routeType`
- `workspaceId`
- `reason`

### `POST /api/auth/session/refresh`

用途：

- 刷新当前登录会话

请求体：

- `refreshToken?`
- `rotateRefreshToken?`

返回：

- `sessionToken`
- `refreshToken`
- `expiredAt`

成功状态码：

- `200`

### `POST /api/auth/logout`

用途：

- 退出当前登录态

鉴权：

- `x-session-token`

请求体：

- `allDevices?`

返回：

- `{ ok: true }`

成功状态码：

- `200`

## 4.2 Onboarding

### `GET /api/onboarding/status`

用途：

- 查询首次建档状态

鉴权：

- `x-session-token`

返回：

- `completed`
- `profileExists`
- `workspaceId`
- `nextStep`

### `POST /api/onboarding/profile`

用途：

- 创建或更新首次建档资料

鉴权：

- `x-session-token`

请求体：

- `nickname`
- `industry?`
- `contentDirection?`
- `targetPlatform?`
- `experienceLevel?`

返回：

- `profile`
- `routing`

成功状态码：

- `200`

## 4.3 Workspace

### `GET /api/workspaces`

用途：

- 获取当前账号的工作空间列表

鉴权：

- `x-session-token`

返回：

- `items`

### `GET /api/workspaces/current`

用途：

- 获取当前默认工作空间

鉴权：

- `x-session-token`

返回：

- `id`
- `name`
- `type`
- `role`
- `status`

## 5. 当前实现状态

本阶段已完成：

- 控制器与路由定义
- DTO 与响应模型
- Swagger 注解
- 领域分层骨架
- 号码认证登录主链路最小实现
- 建档状态与建档提交最小实现
- 工作空间查询最小实现

## 6. 当前已知限制

- 阿里云 H5 号码认证对网络环境有要求，Wi-Fi 下成功率与行为存在限制
- 当前会话仍以 header 传递为主，后续可升级为 cookie + refresh token 组合
- 当前默认当前空间取列表第一项，后续多空间场景需要补“默认空间选择”策略
- 当前阿里云号码认证调用基于 OpenAPI RPC 签名实现，后续可视情况替换为官方 SDK

## 7. 建议的下一步

建议继续按这个顺序推进：

1. 联调阿里云 H5 号码认证前端接入
2. 实测登录闭环
3. 补 session 续期与失效策略
4. 再进入游客试用与 AI 主流程
